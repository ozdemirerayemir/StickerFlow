customtkinter>=5.2.0
Pillow>=10.0.0
rembg[cpu]>=2.0.50
tkinterdnd2>=0.3.0
pytest>=7.4.0
pyinstaller>=6.0.0

# Optional Windows clipboard support (not required; app degrades gracefully)
# pywin32>=306; sys_platform == "win32"
